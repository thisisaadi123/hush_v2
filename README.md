
  # HUSH

  This is a code bundle for Quiz Builder with Scoring. The original project is available at https://www.figma.com/design/AOJs5mvSUzytYcwDUYt71l/Quiz-Builder-with-Scoring.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
